spring:
  application:
    # 服务名称
    name: userservice
  profiles:
    #开发环境，这里是dev
    active: dev
  cloud:
    nacos:
      # nacos服务端地址
      server-addr: localhost:8848
      config:
        # 文件后缀名
        file-extension: yaml